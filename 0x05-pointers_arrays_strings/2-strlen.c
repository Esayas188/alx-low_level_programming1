#include "main.h"
#include <string.h>
/**
 * _strlen -> function to get the lengeth of a string
 * @s: string pointer to passed to this function
 * Return: returns lengeth of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
